Algoritmo "atribuicao"

Var
   x : inteiro
   y : real
   b, c, h, area : real
Inicio
      x <- 5
      y <- 2 * x

      escreval(x)
      escreval(y)
      escreval

      b <- 6.0
      c <- 8.0
      h <- 5.0
      
      area <- (b + c) / 2.0 * h;
      
      escreval(area)


Fimalgoritmo